const lang = 'pt-BR';
const currency = 'BRL';
const formatStyle = 'currency';
const system_name = 'HAR Hotel';
const asyncStorageConfig = {
   user: `@${system_name.replace(' ', ':')}-user`,
};


export {
   lang,
   currency,
   formatStyle,
   system_name,
   asyncStorageConfig
};